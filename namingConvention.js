/**Rules and Naming Conventions */

// 1.Case sensitive
let year=2020;
let Year=2021;

// 2.contains Alphabets(aA-zZ),numbers(0-9),underscore(_),Dollar($)
let name="utsav";
let first_name="utsav";
let amt$=4000;
let num1=10;

// 3.cannot start with a number
let $num=10;

// 4.No Reserved keywords
// let let=10;

// 5.No blank space in between 
// let birth date=10; compiler thinks there should be comma between birth and date
let birthdate=10;

// under 10 or 15 characters

// variable name should always exist on the left-hand
let a=10;
// let 10=b;

/* Naming Conventions */

// Relatable names for readability ->name your variable as readable as possible

// Snake Casing or camel Casing
/**
    birthYearFirstChild ------> camelCasing
    birth_year_first_child --->snake_casing
*/
